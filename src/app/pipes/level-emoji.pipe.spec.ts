import { LevelEmojiPipe } from './level-emoji.pipe';

describe('LevelEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
